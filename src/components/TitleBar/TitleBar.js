import React from 'react'
import './TitleBar.scss'

const TitleBar = ({ }) => {

    return (
        <div className="title-bar">
            <h1>PACMAN</h1>
        </div>
    );
}

export default TitleBar;